export AWS_ACCESS_KEY_ID=${aws_user_map.key_id}
export AWS_SECRET_ACCESS_KEY=${aws_user_map.key_secret}
export AWS_REGION=${region}
export VAULT_ADDR=https://${node_id}.${domain}:${port}
export VAULT_AWSKMS_SEAL_KEY_ID=${aws_kms_map.kms_key_id}
export VAULT_SEAL_TYPE=${seal_type}


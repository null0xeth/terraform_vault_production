[Service]
Environment="AWS_ACCESS_KEY_ID=${aws_user_map.key_id}"
Environment="AWS_SECRET_ACCESS_KEY=${aws_user_map.key_secret}"
Environment="AWS_REGION=${region}"
Environment="VAULT_ADDR=https://${node_id}.${domain}:${port}"
Environment="VAULT_AWSKMS_SEAL_KEY_ID=${aws_kms_map.kms_key_id}"
Environment="VAULT_SEAL_TYPE=${seal_type}"

